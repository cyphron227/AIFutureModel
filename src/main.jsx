import React from 'react'
import ReactDOM from 'react-dom/client'
import LedgerModel from './Ledger'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LedgerModel />
  </React.StrictMode>,
)
